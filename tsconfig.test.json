{
  // 主 tsconfig 的 rootDir 是 src（給 build 用），沒有涵蓋 tests/。
  // 這份只做型別檢查，把測試也納進來，避免測試裡的型別錯誤要到執行期才發現。
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["src/**/*.ts", "tests/**/*.ts", "*.config.ts"]
}
