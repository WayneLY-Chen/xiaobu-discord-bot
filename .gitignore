node_modules/
dist/
*.log

# 機密：絕對不可 commit
.env
.env.*
!.env.example

# SQLite 資料庫（由 Docker volume 持久化）
# 保留空目錄本身，資料檔不進版控
data/*
!data/.gitkeep
*.db
*.db-shm
*.db-wal

# 編輯器 / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# 小步的大頭貼與橫幅原圖。
# 這是這個 Bot 專屬的形象素材，版權保留，不隨公開 repo 散布。
# 自架的人請自行準備圖片（詳見 README「圖片素材」）。
assets/

# Claude Code 的本機權限設定：裡面有正式機的位址，不進公開 repo
.claude/settings.local.json

# 審查代理會在這裡留下權限清單與暫存檔，不進版控
.claude/
